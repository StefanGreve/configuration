pinentry-program /opt/homebrew/bin/pinentry-curses

allow-loopback-pinentry
disable-scdaemon

# default = idle timeout (resets on use); max = absolute lifetime
default-cache-ttl 28800
max-cache-ttl 28800
