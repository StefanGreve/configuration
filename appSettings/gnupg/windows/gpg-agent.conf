allow-loopback-pinentry
disable-scdaemon

# Cache the passphrase for a full workday (seconds) so the pinentry window
# appears at most once per session. default = idle timeout (resets on use);
# max = absolute lifetime regardless of use.
default-cache-ttl 28800
max-cache-ttl 28800
