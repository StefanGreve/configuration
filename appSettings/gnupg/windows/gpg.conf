# Windows uses the graphical pinentry (pinentry-basic) for passphrase entry.
# Do NOT set 'pinentry-mode loopback' here: loopback cannot prompt on the
# Windows console and hangs git commit signing. gpg-agent caches the passphrase
# (see gpg-agent.conf), so the pinentry window appears at most once per session.
